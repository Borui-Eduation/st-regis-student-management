# Dockerfile 用于运行数据导入脚本
# 基于开发环境镜像，包含所有依赖

FROM node:22-alpine

WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm install --legacy-peer-deps

# 复制源代码和脚本
COPY . .

# 设置工作目录
WORKDIR /app

# 默认命令（可以被覆盖）
CMD ["npm", "run", "import:students"]



